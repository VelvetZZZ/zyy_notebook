# 迭代 vs 递归 —— 学习笔记

## 📌 基本定义

### ✅ 迭代（Iteration）
迭代是使用 **循环结构（如 for 或 while）** 来重复执行一段代码直到满足某个条件。

- 特点：不需要函数自己调用自己。
- 控制逻辑明确，依赖变量更新。
- 更接近底层机器执行逻辑。

### 🔁 递归（Recursion）
递归是函数**调用自己本身**来解决问题，每次调用都缩小问题规模，直到遇到“基础情况（Base Case）”。

- 特点：函数自己调用自己。
- 表达简洁、结构清晰，特别适合树形结构等问题。
- 需要消耗栈空间。

---

## 📊 应用场景对比

| 特性               | 迭代                     | 递归                          |
|--------------------|--------------------------|-------------------------------|
| 使用频率           | ✅ 高                     | 较低                          |
| 性能               | ✅ 更高（无栈开销）      | 低（存在函数调用栈开销）     |
| 栈溢出风险         | ❌ 无                     | ✅ 有                         |
| 可读性             | 一般                     | ✅ 表达简洁（适合树/图）     |
| 算法表达力         | 普通                     | ✅ 强（回溯、分治等）        |
| 工程开发推荐       | ✅ 是                     | ❌ 慎用（仅小问题或配合尾递归优化） |
| 典型应用场景       | 循环处理数据、大数据处理 | 树遍历、DFS、数学归纳法等     |

---

## 💡 现代编程中的实践建议

- **实际开发中优先使用迭代**，尤其是处理大量数据或性能关键任务。
- **递归适合教学、算法设计、数据结构（如树/图）处理**。
- 如果使用递归，请确保有合理的**终止条件**并避免**深度过大**导致栈溢出。

---

## 🧠 一句话总结

> **迭代更“实用”和“工程导向”；递归更“理论性”和“表达力强”。**

---

## 📎 示例（以阶乘计算为例）

### Python 递归实现：

```python
def factorial(n, k):
    if n == 0:
        return k
    else:
        return factorial(n - 1, k * n)
```

### Python 迭代实现：

```python
def factorial(n, k):
    while n > 0:
        n, k = n - 1, k * n
    return k
```